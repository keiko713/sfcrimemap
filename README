This application was made using the Google Maps JavaScript API v3.
The mapdata.json was created from SFPD Crime Data (originally in CSV format) using parser.py.
The application reads the JSON and shows the markers on the map.
